from keras.models import load_model
from Tester import TestModel

PathToImages = '../../1100downloaded_vII/'
LocalFolder = ''

# Complicated loading of model

#model = load_model(LocalFolder+'data/.h5')
#TestModel(model, LocalFolder, PathToImages)